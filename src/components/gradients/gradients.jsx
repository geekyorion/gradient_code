import React from 'react';

export default class Gradients extends React.Component {
    render() {
        return(
            <div>Gradient component is successfully added to the application</div>
        );
    }
}